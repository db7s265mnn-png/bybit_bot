# Copy to .env in the project root. Never commit the real file.
# See config/.env.example for the same variables.

BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=true
MODE=paper
LIVE_TRADING_CONFIRM=false
